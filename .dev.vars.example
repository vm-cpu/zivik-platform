# Local secrets for `npm run cf:dev` (copy to .dev.vars).
# Deploy hook the admin calls on publish; leave unset locally.
# DEPLOY_HOOK_URL=https://api.cloudflare.com/client/v4/workers/builds/deploy_hooks/<id>
